pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway_Way to Reinvent Yourself first'
                sh 'python3 --version'
                sh 'python3 pipeline.py'
            }
        }
    }
}